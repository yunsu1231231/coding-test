### 1. 핵심
queue는 앞, 뒤에서 둘 다 O(1)  
배열은 앞에서는 O(N)  

### 2. 다른 풀이
```declarative

// 1. 버리고 + 뒤로 보내는것을 하나로 묶기 
// 조건 기반 분기점 사고할 때 -> 두 동작이 수행되어야 하나의 카드로 줄어드니깐: 하나의 과정으로 묶기
while(q.size() > 1){
    q.poll();
    q.offer(q.poll());
}


```